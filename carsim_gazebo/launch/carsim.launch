<launch>
  <!-- We resume the logic in empty_world.launch, changing only the name of the world to be launched -->
  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find carsim_gazebo)/worlds/empty.world"/>
    <!-- more default parameters can be changed here -->
  </include>
<!-- Spawn a robot into Gazebo -->
<node name="spawn_urdf" pkg="gazebo_ros" type="spawn_model" args="-file $(find carsim_discription)/urdf/model.urdf -urdf -z 1 -model carsim" />
<!-- ros_control mybot launch file -->
<!--
<include file="$(find carsim_control)/launch/carsim_control.launch" />
-->
</launch>
